# 2026-02-10 (Monday)

## 🤖 BOS-AI as Operating System for Ace

Jamie decided to have Ace use the BOS-AI framework as an operating system for running AISearchMastery business. Instead of selling BOS-AI as a product, it becomes the structured methodology Ace follows.

**BOS-AI provides 6 agent roles:**
- Marketing: Brand Strategy, Campaign Execution, Content Creation
- Sales: Pipeline Management, Conversion Optimization, Revenue Operations

**Created briefing:** `/home/ubuntu/clawd/aisearch/BOS-AI-BRIEFING.md`

**Action items for Ace:**
1. Update SOUL.md with BOS-AI operating system section
2. Create memory structures (`memories/business/`, `memories/strategies/`, `memories/lessons/`)
3. Create AISearchMastery foundation docs (Marketing Bible, etc.)
4. Operationalize: think in Business Chassis multipliers and customer journey stages

Ace already reviewed the LLMtxt Mastery docs created with BOS-AI (Marketing Bible, Marketing Plan, Content Calendar).

---

## 🚀 Opus 4.6 Upgrade (Ace First)

Upgraded Ace to Claude Opus 4.6 as a test before migrating Marvin.

**Config changes:**
- Added model alias: `claude-opus-4-6` → `opus46`
- Set Ace's model to `opus46`
- Marvin stays on `opus` (Opus 4.5)
- Auth profile unchanged: `claudecode` (Max subscription, not API)

**Key lesson from Sunday's failed attempt:**
- Don't change `auth.profiles` — keep `provider: "claudecode"`
- Only change model references
- The Feb 8 incident was caused by switching to API mode syntax

**Plan:** If Ace is stable for 24h, upgrade Marvin to opus46 too.

---

## ⚡ The Real-Time Rule (Learned from Ace)

New principle adopted — update files **immediately**, not batched at end:

1. New task identified → Add to TASKS.md immediately
2. Task completed → Update TASKS.md immediately
3. File updated → Save immediately
4. End of work → Update memory file

**Why:** Context can be lost mid-session. If you batch for "later," later might never come.

Added to AGENTS.md.

---

## 🧠 Memory Retention Settings Enabled

Jamie noticed context loss issues. Enabled two features to improve retention:

1. **Memory Flush** (`compaction.memoryFlush.enabled: true`)
   - Before compaction, extracts and saves critical info to memory files
   
2. **Session Memory Search** (`memorySearch.enabled: true`)
   - Sources: `["memory", "sessions"]` — indexes both memory files AND conversation history
   - Provider: OpenAI (dedicated API key for embeddings only, stored in config)

Config applied and gateway restarted.

---

## 🧹 Overnight Healthcheck Added

After browser zombie process caused issues (8-day-old Chrome process from Feb 2), added comprehensive healthcheck to OVERNIGHT.md:

**New checks added:**
- **Zombie browser processes** — kill stale Chrome (>24h old)
- **Orphaned node processes** — find/kill runaway scripts
- **Stale exec sessions** — review background processes
- **/tmp cleanup** — delete working files >7 days old
- **Session transcript size** — alert if >500MB per agent
- **Health report** — include before/after disk usage in summary

**Root cause of today's issue:** Chrome process from Feb 2 survived gateway restarts but became unresponsive. Browser control server showed "running" but snapshots timed out. Fix: `pkill -f "chrome.*18800"` then restart browser.

---

## 📝 Earlier Today (from truncated context)

- Published "The Day I Overwrote Myself" blog post
- Fixed Ace not responding issue (group messages require @mention)
- Mission Control API field name fixes deployed
- Published Trader-7 stop-loss guardrails blog across all platforms
- Created `jamie-publishing` skill for publishing patterns

---

## 💬 Social Engagement Monitor (Priority Task Added)

Jamie requested automated comment monitoring for LinkedIn and X posts:
- Check posts for new comments/replies
- Generate suggested responses (respectful, fun, on-brand)
- Schedule: 6 AM, 12 PM, 6 PM UTC (3x daily)

Added to TASKS.md as priority item. Will need:
- LinkedIn access (browser automation or API)
- X API access (or scraping fallback)
- Response generation with tone guidelines
- Cron jobs for scheduled checks

---

## 🔧 Ace Reverted to Opus 4.5

Opus 4.6 test failed. Reverted Ace back to Opus 4.5 via config patch. Reason for failure unknown (Jamie didn't specify symptoms).
